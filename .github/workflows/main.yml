name: Keep Render Services Alive

on:
  schedule:
    # Пн-Пт: 10:00–22:00 МСК (07:00–19:00 UTC) каждые 10 минут + финальный пинг в 19:00
    - cron: '*/10 7-18 * * 1-5'
    - cron: '0 19 * * 1-5'
    # Сб-Вс: 10:00–22:00 МСК (07:00–19:00 UTC) каждые 10 минут + финальный пинг в 19:00
    - cron: '*/10 7-18 * * 0,6'
    - cron: '0 19 * * 0,6'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping MCP Hub (Root Endpoint)
        continue-on-error: true
        run: |
          echo "=== $(date -u '+%Y-%m-%d %H:%M:%S UTC') ==="
          HTTP_CODE=$(curl -s -o /tmp/mcphub_response.txt -w "%{http_code}" \
            --max-time 120 \
            --retry 3 \
            --retry-delay 15 \
            https://xiaozhi-mcphub-deploy-server.onrender.com/)
          
          echo "MCP Hub → HTTP $HTTP_CODE"
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ MCP Hub is alive"
            cat /tmp/mcphub_response.txt | head -c 200
            echo ""
          else
            echo "❌ MCP Hub returned HTTP $HTTP_CODE"
            cat /tmp/mcphub_response.txt | head -c 500
            exit 1
          fi

      - name: Ping Qdrant MCP Server
        continue-on-error: true
        run: |
          echo "=== $(date -u '+%Y-%m-%d %H:%M:%S UTC') ==="
          HTTP_CODE=$(curl -s -o /tmp/qdrant_response.txt -w "%{http_code}" \
            --max-time 120 \
            --retry 3 \
            --retry-delay 15 \
            https://qdrant-mcp-server.onrender.com/)
          
          echo "Qdrant MCP → HTTP $HTTP_CODE"
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Qdrant MCP is alive"
          else
            echo "❌ Qdrant MCP returned HTTP $HTTP_CODE"
            cat /tmp/qdrant_response.txt | head -c 500
            exit 1
          fi
